# Workspace Rules

## Automatic Git Push on Completion
- After completing any requested code or layout modifications, always run `git add .`, `git commit -m "<descriptive message>"`, and `git push origin main` so that changes are immediately reflected on the live deployed URL.
